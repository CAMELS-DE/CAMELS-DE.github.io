import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'camels-de.github.ioblank',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
